package ${pkgPath};

import ${servicePath};
import ${daoPath};
import ${beanPath};
import com.mdks.common.base.BaseDAO;
import org.springframework.stereotype.Service;
import com.mdks.common.base.AbstractServiceImpl;
import javax.annotation.Resource;
import java.util.List;
import java.util.Map;

/**
 * @Description:${moduleNameCN}业务层实现类
 * @ClassName:  ${className}ServiceImpl
 * @author: sunshine  
 */
 @Service("${lcfClassName}Service")
public class ${className}ServiceImpl extends AbstractServiceImpl<${className}> implements I${className}Service {
    
    @Resource
	private ${className}Dao ${lcfClassName}Dao;
	
	@Override
	public BaseDAO<${className}> dao() {
		return ${lcfClassName}Dao;
	}
	
    @Override
	public void setId(${className} t, String id) {
         t.set${capitalizeKey}(id);
	}
	
	@Override
	public Integer insert(${className} ${lcfClassName}) {
		return super.insert(${lcfClassName});
	}

	@Override
	public Integer update(${className} ${lcfClassName}) {
		return super.update(${lcfClassName});
	}

	@Override
	public ${className} findObjectById(String id) {
		return super.findObjectById(id);
	}

	@Override
	public List<${className}> findList(Map<String, Object> map) {
		return super.findList(map);
	}

	@Override
	public List<${className}> queryList(${className} ${lcfClassName}) {
		return super.queryList(${lcfClassName});
	}

	@Override
	public Integer delete(String[] ids) {
		return super.delete(ids);
	}

	@Override
	public Integer validate(${className} ${lcfClassName}) {
		return super.validate(${lcfClassName});
	}
}